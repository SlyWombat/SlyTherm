# House convention (house-network-ops #135 / #15): failures page through the
# shared handler. install.sh only installs this drop-in when
# backup-alert@.service exists on the host.
[Unit]
OnFailure=backup-alert@%n.service
