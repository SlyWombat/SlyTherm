// thermostat_secrets.h.example — template for src/thermostat_secrets.h.
//
//   cp src/thermostat_secrets.h.example src/thermostat_secrets.h
//
// then fill in your values. src/thermostat_secrets.h is GITIGNORED — never
// commit real credentials (same rule as .env, see docs/05 "PlatformIO setup").
// If the file is absent the firmware builds with these empty defaults and
// skips Wi-Fi (serial-only bench mode).

#pragma once

#define THERMOSTAT_WIFI_SSID ""
#define THERMOSTAT_WIFI_PASS ""
#define THERMOSTAT_MQTT_HOST ""
#define THERMOSTAT_MQTT_PORT 1883
#define THERMOSTAT_MQTT_USER ""
#define THERMOSTAT_MQTT_PASS ""
