// remote_secrets.h.example — template for src/remote_secrets.h.
//
//   cp src/remote_secrets.h.example src/remote_secrets.h
//
// then fill in your values. src/remote_secrets.h is GITIGNORED — never
// commit real credentials (same rule as the Controller's
// thermostat_secrets.h). This is a #96 P0 bring-up shortcut (prove the
// ESP32-C6 hosted WiFi link associates + gets an IP) — real Remotes will
// provision on-device via the touchscreen Wi-Fi settings screen (#100/#101,
// reusing the Controller's wifi_prov module), not a compiled-in secret.

#pragma once

#define REMOTE_WIFI_SSID ""
#define REMOTE_WIFI_PASS ""
